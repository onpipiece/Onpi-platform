# Example environment variables for the ONPI platform
# Copy to a file named `.env` locally, fill values and do NOT commit the real `.env` to git.

NODE_ENV=production
APP_URL=https://onpi-platform.vercel.app
DEBUG_PASSWORD_RESET=0

# Database (choose one):
MONGO_URI=

# Supabase (optional, alternative to MongoDB):
SUPABASE_URL=
SUPABASE_KEY=

# SMTP (for password reset emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=0
SMTP_FROM=

# Local dev only (optional)
PORT=3000

# Notes:
# - On Vercel, upload your real `.env` via Project → Settings → Environment Variables → Import .env
# - Mark secrets (MONGO_URI, SUPABASE_KEY, SMTP_PASS, etc.) as secret in Vercel.PORT=3000
NODE_ENV=production
APP_URL=https://your-app-url.example

# Supabase (optional - leave empty if using Mongo)
SUPABASE_URL=
SUPABASE_KEY=

# MongoDB (optional - use if you switch to Mongo)
# Replace <db_password> with your actual database password. Do NOT commit the real password.
MONGO_URI=mongodb+srv://onpipiece_db_user:<Scarface020.>@cluster0.kwv1wa7.mongodb.net/?appName=Cluster0

# SMTP (optional - for password reset emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_SECURE=0

DEBUG_PASSWORD_RESET=0
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key
